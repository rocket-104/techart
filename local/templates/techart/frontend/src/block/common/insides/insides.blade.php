<div class="{{ $block }}">
	<a class="{{ $block->elem('linkHeader') }}" href="#">{{ $headerInsides }}</a>
	<div class="{{ $block->elem('blockForContent') }}">

		{% for (i=0; i<2; i++) %}
		{!! $inside[]>] !!}
		{% endfor %}
	</div>
</div>